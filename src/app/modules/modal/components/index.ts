import { LeftSideModalComponent } from './left-side-modal';
import { RightSideModalComponent } from './right-side-modal';

export const ComponentsExport = [RightSideModalComponent. LeftSideModalComponent];

export const Components = [...ComponentsExport];
